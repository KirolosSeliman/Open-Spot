export type Locale = "en" | "fr";

export type Dictionary = {
  common: {
    productName: string;
    tagline: string;
  };
  navigation: {
    dashboard: string;
    pricing: string;
    contact: string;
    waitlist: string;
  };
};
